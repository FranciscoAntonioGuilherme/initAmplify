version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - mkdir -p build
        - cp -r src/* build/
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: []
